#!/usr/bin/env python3
"""
Spark Agent CLI launcher.

This wrapper should behave like the installed `spark` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from spark_cli.main import main
    main()
