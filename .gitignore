/venv/
/dev/
/_pycache/
*.pyc*
__pycache__/
.venv/
.vscode/
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
export*
logs/
data/
.pytest_cache/
.ruff_cache/
.mypy_cache/
tmp/
temp_vision_images/
temp_*/
spark-*/*
examples/
tests/quick_test_dataset.jsonl
tests/sample_dataset.jsonl
run_datagen_kimik2-thinking.sh
run_datagen_megascience_glm4-6.sh
run_datagen_sonnet.sh
source-data/*
run_datagen_megascience_glm4-6.sh
data/*
node_modules/
browser-use/
agent-browser/
# Private keys
*.ppk
*.pem
privvy*
images/
__pycache__/
*.egg-info/
wandb/
testlogs

# CLI config (may contain sensitive SSH paths)
cli-config.yaml

# Skills Hub state (lives in ~/.spark/skills/.hub/ at runtime, but just in case)
skills/.hub/
ignored/
.worktrees/
environments/benchmarks/evals/

# Python build artifacts
dist/
build/
*.egg-link
*.spec.bak

# Test coverage
.coverage
coverage.xml
htmlcov/

# The production Web UI bundle is intentionally committed. Existing Spark
# versions load their updater before `git pull`, so carrying a complete,
# coherent bundle is what makes the first upgrade to newer updater logic safe.

# Release script temp files
.release_notes.md
mini-swe-agent/

# Nix
.direnv/
result
website/static/api/skills-index.json

# Tauri / desktop build artifacts
src/spark_cli/web/src-tauri/target/
src/spark_cli/web/src-tauri/gen/
# Frozen PyInstaller --onedir sidecar, staged here by build_desktop.sh
src/spark_cli/web/src-tauri/resources/
# DMG: built locally, uploaded via /release-mac — never commit (~400MB)
Spark.dmg

# Local release-signing config (Developer ID identity + notary profile name).
# Sourced by build_desktop.sh. Never commit — use scripts/release.env.example.
scripts/release.env
